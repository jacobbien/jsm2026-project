# Get indices of nonzero elements of a vector ordered from largest to smallest

This is like `which(vec != 0)`, but with indices sorted by size of
`vec`.

## Usage

``` r
ordered_nz(vec)
```

## Arguments

- vec:

  a numerical vector
